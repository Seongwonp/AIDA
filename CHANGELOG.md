# Changelog

## 2026-09-05

웹을 제품처럼 만들고, 자기 정제 아이디어에 마침표를 찍었다.

### Added

- **랜딩 페이지** (`Landing.tsx`). 큰 활자·네온 형광펜·흐르는 띠·세어 올라가는
  숫자·근거 막대. 여기 적힌 숫자는 전부 `experiment/`에서 실제로 잰 값이다
  (94.0% / 27종 / 10.2σ). 지어낸 숫자를 쓰면 고객이 리포트를 열었을 때 화면
  수치와 어긋나 오히려 신뢰를 잃는다.
- **재검수 목록을 도구로** (`ReviewQueue.tsx`). 유형 필터, 이미지 검색,
  "안 본 것만", 검토 체크와 진행률, CSV 내려받기(엑셀 한글 대응 BOM).
  검수 진행은 브라우저에만 남긴다 — 서버에 저장하려면 사용자 개념이 필요한데
  지금 제품에는 없다.
- **방법론 카드** (`MethodCard.tsx`). "정답 없이 어떻게 판단하나 / 기준 모델이
  틀리면 어떻게 되나 / 얼마나 믿을 수 있나 / 무엇을 못 하나". 지표와 차트만
  있으면 고객이 진단 결과를 실제보다 확정적인 것으로 읽는다.
- **다크 모드.** 색을 전부 토큰으로 빼고 시스템 설정을 기본값으로 삼되 고른
  선택을 기억한다. 네온 위 글씨는 다크에서도 검정 고정 — 토큰을 그대로 쓰면
  흰 글씨가 얹혀 안 보인다.
- **진단 진행 표시.** 추론이 수십 초 걸리는데 버튼 글자만 바뀌어 화면이 죽은
  것처럼 보였다. 가짜 진행률 막대는 쓰지 않는다.
- 규모 실험 도구: `run_refine_scale.sh`, `run_refine_scale_mc.sh`,
  `build_clean_subset.py`, `--select nested`, `AIDA_VAL_HOLDOUT`

### 검수를 실제로 할 수 있게 (오후)

- **지난 진단을 다시 연다.** 새로고침하면 결과가 사라졌다. 디스크에는 그대로
  있는데 화면에서 갈 방법이 없어 다섯 개가 쌓인 채 아무도 못 봤다. 검수는 한
  번에 끝나지 않는다 — 반쯤 보다가 내일 이어 보는 게 정상이고(판정을
  브라우저에 남기는 이유도 그것이다) 그러려면 어제 것을 열 수 있어야 한다.
  `GET /api/datasets/history`. ~~**경로 순서가 중요하다**: `/{dataset_id}/...`
  보다 먼저 선언하지 않으면 FastAPI가 "history"를 dataset_id로 읽는다.~~
  — **정정(같은 날):** 사실이 아니다. `/{dataset_id}/...` 는 전부 두 조각이고
  `/history`는 한 조각이라 서로 가릴 수 없다. 선언을 맨 끝으로 옮겨 확인했더니
  그대로 200이다. 그때 본 404는 서버가 새 코드를 아직 안 읽은 것이었는데,
  원인을 확인하지 않고 그럴듯한 설명을 붙였다.
- **키보드로 판정한다.** 판정 버튼을 만들어 놓고 써보니 느리다. 100건이면
  마우스를 100번 옮겨야 하고 미리보기를 켜면 스크롤까지 따라다닌다.
  `j`/`k` 이동, `f` 오류, `d` 아님, 판정하면 다음 줄로. 입력칸에 글자를 치는
  중에는 안 먹는다 — 이미지 이름에 'd'가 들어갈 때마다 판정이 찍히면 안 된다.
  "안 본 것만"이 켜져 있으면 판정한 줄이 사라지므로 커서를 그대로 둔다.
  평소처럼 +1 하면 한 건씩 건너뛴다.
- **조건 표를 정렬한다.** 27줄을 조건 이름 순으로만 보여줬다. 이 표가 답해야
  할 질문은 "어떤 오류가 제일 아픈가"인데 눈으로 27줄을 훑으라는 뜻이었다.
  숫자 열은 첫 클릭에 큰 값부터 — 찾는 건 대개 "제일 많이 떨어진 것"이다.
  아직 안 잰 값(null)은 방향과 무관하게 아래로 보낸다.
- **지운다.** 이력을 꺼내 놓고 보니 지우는 길이 없었다. 용량(다섯 개에
  178MB)보다 **고객 이미지가 서버에 무기한 남는 것**이 문제다.
  `DELETE /api/datasets/{id}`. 되돌릴 수 없으니 줄 안에서 한 번 더 묻는다 —
  `window.confirm`은 어느 데이터셋인지 못 보여주고 브라우저마다 생김새가 다르다.
  경로는 한 조각짜리 이름인지 보고 부모가 정말 UPLOADS_DIR인지 다시 확인한다.
- CSV에 박스 좌표를 넣었다. 받는 쪽이 라벨링 도구인데 이미지 이름만으로는 어느
  박스인지 못 찾는다 — 한 장에 스무 개가 들어 있는 게 보통이다.

### 처음 온 사람이 가장 먼저 걸리던 곳

- **폴더째 압축한 zip을 거절하고 있었다.** 윈도우도 맥도 폴더를 우클릭해
  압축하면 안이 `mydata/images/...` 로 한 겹 싸인다. 가장 흔한 방식인데
  "images/ 폴더가 있어야 합니다"로 막았다 — 사용자 눈에는 분명히 있는 걸
  없다고 하는 셈이다. 한 겹을 벗기되 **벗기면 안 되는 경우는 건드리지
  않는다**(최상위 폴더가 둘 이상, 안에도 images/가 없음). 잘못 벗기면 원인이
  더 가려진다.
- `labels/`에 `.txt`가 하나도 없으면 거절한다. 0건으로 통과시키면 모든 라벨이
  '누락 의심'으로 나와서 진짜 원인(.json·.xml을 넣었다)을 못 찾는다.
- 에러 문구의 "클래스는 1개만 지원"을 지웠다. 다중 클래스 프로파일이 생긴
  뒤로 사실이 아니다.

### Changed

- **제품을 앞으로 꺼냈다.** 한 페이지에 카드 일곱 장이 쌓여 있었고 실제로 쓰는
  "내 데이터셋 진단"이 3778px 아래 맨 마지막이었다. 랜딩 → `진단`/`근거` 두 탭.
- 기준 실험(KITTI 412장) 요약이 진단 탭에 있어 **자기 데이터 점수로 읽혔다.**
  근거 탭으로 옮기고 제목에 "기준 실험 데이터셋"을 박았다.
- 카드에 머리를 만들어 위계를 줬다. 헤더 고정, 반응형, 모션 감소 존중.

### Fixed

- `train.py`/`evaluate.py`가 정제 조건을 이름으로 못 찾던 것 — 조건 목록이
  새 조건군을 놓치는 **네 번째** 같은 사고. `_BY_NAME`을 쓰게 했다.
- `clean_sub` 대조군을 **만드는 코드가 아예 없었다**(선언과 읽기만 있었다).
- KITTI 다운로드가 이미지당 2.9초로 3시간 걸리던 것 → 병렬 6연결로 22분.
  중간에 서버가 연결을 끊는 일이 실제로 있어 재시도도 넣었다.
- `download_kitti.py`만 stdout 인코딩 재설정이 없어 cp949에서 죽던 것

### 자기 정제에 마침표 (docs/21 AJ)

X는 점 두 개로 "교환 비율이 7배 좋아졌다, 이 추세가 이어지면 어느 규모에서는
뒤집힌다"고 적었다. 네 점으로 다시 그으니 반대다:

| 규모 | 정제로 얻을 천장 | 절반 버리는 손해 | 비율 |
|---|---|---|---|
| 400장 | 0.041 | 0.071 | 0.58배 |
| 800장 | 0.035 | 0.062 | 0.56배 |
| 1600장 | 0.014 | 0.058 | 0.24배 |
| 3200장 | 0.025 | 0.053 | 0.47배 |

**규모가 8배 커지는 동안 비율이 오르내릴 뿐 1에 가까워지지 않는다.**
자기 정제는 규모를 키워 살릴 수 있는 아이디어가 아니다.

천장(두 학습 결과의 차이)에 붙는 노이즈가 ±0.026인데 천장 네 개의 폭이
0.027이다 — **규모별 천장 차이는 측정된 것이 아니라 흔들림이다.** 반면
손해는 어느 규모에서나 노이즈의 2배 이상이다. 잃는 쪽은 확실히 재졌고
얻는 쪽은 잴 수 없을 만큼 작다.

세 점(0.58/0.56/0.24)만 보고 "줄어든다"고 썼다가 네 번째 점에서 지웠다.
점 두 개로 직선을 그은 X를 지적하면서 점 세 개로 같은 실수를 할 뻔했다.

X의 교란(400장과 800장의 프레임 구성이 달랐다)도 없앴다 — 4000장을 한 번 섞은
순열에서 앞부터 잘라 중첩 부분집합을 만들고 평가셋을 끝 800장으로 고정했다.

### 제품 — 재검수 목록이 도구가 됐다

- **문제 박스를 눈으로 본다.** 목록은 "000075.png의 1번 박스가 28% 작다"까지만
  알려줬고 검수자가 직접 이미지를 열어야 했다. 박스 주변만 잘라 네온 테두리로
  그린다 — 전체를 축소하면 작은 상자는 몇 픽셀이 되어 안 보인다.
  좌표는 `BoxFinding.box`에 원래 있었는데 JSON에 안 내보내고 있었다.
- **검수 결과를 남기면 이 데이터셋의 실제 정밀도가 나온다.** "봤다"만 체크하면
  무엇을 알아냈는지 사라진다. 오류/아님으로 판정하면 "2/4 판정 · 실제 오류 50%"
  처럼 **고객 자기 숫자**가 나온다 — 우리가 KITTI에서 잰 94.0%가 아니라.
- **기준 모델이 이 데이터를 보고 있는지 잰다.** AA·AD·AG가 세 번 같은 말을
  했는데도 추천은 "클래스를 덮는가"만 봤다. 정답 없이 재지는 신호(라벨 중
  예측과 짝지어진 비율)로 대신한다 — COCO 데이터에서 맞는 자 85.1%,
  KITTI 자 31.9%로 뚜렷하게 갈린다. 절반 미만이면 경고한다.

### 조용히 틀리는 것들을 또 찾았다

- **린트가 아무것도 안 보고 있었다.** 설정에 규칙이 두 개뿐이라 미사용 변수도
  `==` 비교도 검사 대상이 아니었다. 어제 타입체크(`files: []`)에 이어 두 번째다.
  규칙을 켜자 실제 문제 2건이 나왔다(렌더마다 새로 만들어지는 기본값 배열,
  배열 인덱스 key).
- **COCO 프로파일이 KITTI 자를 조용히 열 뻔했다.** 경로가 클래스 구성만 보고
  정해져서, 클래스가 `["Car"]` 하나인 COCO 프로파일이 `runs/`를 가리켰다.
  엉뚱한 자로 진단해도 오류가 안 난다.
- **`match_boxes`의 3튜플을 `len()`으로 셌다.** 짝 비율이 127%로 나오고 서로
  다른 두 자가 똑같은 값을 보여서 알았다. 100%를 넘지 않았으면 지나쳤을 것이다.
- 화면 라벨이 판정 유형을 전부 덮는지 검사를 추가했다. 다중 클래스를 넣으며
  `class_mismatch`를 만들었을 때 실제로 어긋났던 적이 있다.

### 문서

- **docs 23개 전체를 훑었다.** 대부분 2026-07에 멈춰 있었는데 성격이 갈린다 —
  원리 설명은 고쳐야 하고 대회 준비 기록은 **고치면 안 된다**(그때의 판단이
  사라진다). 인덱스를 넷으로 나누고, 16개 문서 첫머리에 시점을 밝혔다.
- **`09-getting-started.md`를 새로 썼다.** 새 세션 진입점인데 "2026-07-08 기준,
  다음은 PPT 작업" 상태였다 — 이어받는 사람이 가장 먼저 읽는 문서가 가장
  낡아 있었다. 지금 상태와 **반복해서 난 사고 다섯 가지**를 담았다.
- `docs/21`(3454줄, 35절)에 목차. **결론이 뒤집힌 절 4개를 목차에 표시**한다.
- README 제목의 "MVP 데모"를 "연구 프로토타입"으로 바꾸고, 되는 것과 안 되는
  것을 적었다. 가장 중요한 항목: **기준 모델이 고객 데이터에 맞지 않으면 상위
  10% 정밀도가 94.0%에서 26.0%까지 떨어지는데, 이 프로젝트가 아직 답이 없다.**

## 2026-09-04

기준 모델("자")을 제품에 드러내고, 그 자가 얼마나 믿을 만한지 시드를 늘려
다시 쟀다. **이 과정에서 앞선 결론 세 개를 스스로 뒤집었다.**

### Added

- **어느 기준 모델로 진단했는지 결과에 남긴다** (docs/21 AE).
  가장 큰 구멍은 "자가 모르는 클래스"였다 — Car만 아는 자에 4클래스 데이터를
  주면 나머지 라벨은 오탐으로도 안 나오고 **아예 검사되지 않는다.** 화면에
  흔적이 없어 "문제 없음"과 구별되지 않았다.
  - `backend/app/models.py`: `RulerInfo` (프로파일·아는 클래스·클래스 대조
    여부·학습 시드 산포·모르는 클래스 인덱스)
  - `backend/app/routers/upload.py`: `ruler.json` 사이드카로 남긴다.
    진단 응답에만 담으면 나중에 결과를 다시 열었을 때 자가 사라진다.
  - 업로드 직후 **데이터를 덮는 자 중 가장 좁은 것**을 추천한다. 사용자가
    직접 고른 프로파일은 건드리지 않는다.
  - `frontend`: "무엇으로 쟀는가" 블록, 모르는 클래스 경고
- **진짜 도메인 이동 실험** (docs/21 AI). COCO val2017을 KITTI와 같은
  형식·분할·오류 조건으로 만들어, KITTI 자로 진단했다.
  - `experiment/download_coco.py`, `prepare_coco.py`, `bench_rulers_on_coco.py`
  - `experiment/config.py`: `AIDA_DATASET` 차원 — 없으면 COCO 산출물이 KITTI
    결과를 조용히 덮는다
- **분석 도구**: `analyze_ruler_spread.py`(자별·유형별 산포),
  `pairwise_rulers.py`(모든 쌍의 간격을 σ로), `recompute_without.py`(저장된
  조건별 점수에서 조건군을 빼고 재집계), `check_confidence_margin.py`,
  `check_prediction_drift.py`
- **저장소 이전 도구**: `move_to_drive.py`(심볼릭 링크를 보존하며 이동),
  `relink_duplicates.py`(복사본이 된 것을 내용 대조 후 링크로 환원)

### Changed

- `RULER_SEED_SPREAD_PP`를 `{1: 0.46, 4: 4.80}` → `{1: 2.59, 4: 5.45}`로
  갱신. **화면에 틀린 수치를 띄우고 있었다.**
- 신뢰도 표에 "아예 다른 데이터셋" 열 추가. 기존 값은 같은 KITTI 안에서
  프레임 구성만 바꿔 잰 것이라 낙관적이다.
- `compare_rulers_seeded.py`가 `clean` 조건을 기본으로 뺀다 — 오류를 주입하지
  않아 상위 10% 정밀도가 구조적으로 항상 0%다.
- 실험 데이터 13GB를 D 드라이브로 옮기고 디렉터리 정션으로 연결.
  코드 수정 없이 동작한다(C: 18.5GB → 7.3GB).

### Fixed

- **`git reset --hard`가 심볼릭 링크를 텍스트 파일로 복원한 손상 복구**
  (docs/21 AC). ultralytics가 이런 파일을 "corrupt"로 세고 조용히 건너뛰어,
  학습이 거의 빈 데이터로 돌면서도 성공으로 끝났다. 이미지 153,800 +
  라벨 11,800 복구. `symlink_files`가 깨진 파일을 덮어쓰도록,
  `check_consistency`가 이미지·라벨 둘 다 잡도록 고쳤다.
- `error_injector`가 `.png`를 하드코딩해 COCO(.jpg)를 못 읽던 것 —
  `find_image()`로 한 곳에서 해결
- `compare_rulers_seeded --all-conditions`가 단일 클래스인데도 `class_swap`을
  넣어 죽던 것 — 조건 목록이 클래스 구성을 안 따라가는 반복 사고 유형
- `compare_rulers_seeded`가 조건별 점수를 계산해놓고 버려서, 유형별 산포를
  내려면 GPU로 다시 돌려야 했던 것

### 스스로 뒤집은 결론

1. **AD "먼 이동 자가 약한 이동 자보다 낫다"(2.0σ)** → 시드 7개에서 0.3σ로
   묻혔다. AD가 뽑은 세 시드가 하필 낮은 쪽이었다 (docs/21 AF).
2. **AF "좁은 자가 열 배 안정적"** → 자 4종을 다 재보니 가장 안정적인 게
   4클래스 자기 도메인 자였다(±2.20). 안정성을 정하는 건 클래스 폭이 아니라
   데이터와의 궁합이다 (docs/21 AG).
3. **AG "잘 맞는 자는 예측이 결정 경계에서 멀다"** → 문턱 근처 비율은
   안정성 순서와 안 맞았다. 진짜 기제는 "재학습해도 신뢰도가 잘 안
   움직인다"였고, 그쪽은 자 4개 모두 순서가 맞았다 (docs/21 AH).

### 확정된 수치 (조건 29개, 시드 7개, docs/21 AG)

| 자 | 상위 10% | 표준편차 |
|---|---|---|
| 자기 도메인 (4C) | 94.0% | ±2.20 |
| 먼 이동 (Car 1C) | 65.8% | ±2.59 |
| 약한 이동 (4C) | 64.6% | ±5.45 |
| 넓은 자 (4C 800장) | 61.3% | ±3.27 |

자기 도메인 자의 우위가 7.1~11.8σ로, 이 프로젝트에서 가장 단단한 결과다.

### 진짜 도메인 이동에서는 훨씬 심하다 (docs/21 AI)

COCO 조건을 COCO 자와 KITTI 자로 각각 진단: **82.4% vs 26.0%** (10.2σ).
KITTI 안에서 잰 이동(94.0% vs 64.6%)의 두 배 격차다. 26.0%는 재검수
목록으로 쓸 수 없는 수준이다.

유형별로는 **누락만 확실히 살아남고**(94.9% → 76.7%), 중복은 절반이
깎이며(82.6% → 40.8%), **기하 오류는 전멸한다**(74~90% → 7~29%).
V·Y·Z·AA에서 "중복은 모든 실패 방식을 견딘다"고 한 것은 그 실패 방식들이
전부 KITTI 안의 것이었기 때문이다.

## 2026-09-03

자를 바꿔가며 진단하는 실험들(도메인 이동)과 다중 클래스 3-seed 검증.

### Added

- **도메인 이동 실험** (docs/21 Y·Z·AA). 같은 데이터를 자만 바꿔 진단했다.
  - 자기 도메인 자 92.4% vs 약한 이동 자 65.5% — 도메인이 어긋나면 유형마다
    다르게 무너진다
  - **먼 이동(Car 1클래스) 자가 오히려 나았다**(77.7%) — 갈리는 축은 "얼마나
    먼가"가 아니라 "모르는 것을 아는가"다. 좁지만 확실한 자가 넓지만 흐릿한
    자보다 낫다
  - "넓고 강한 자"를 만들려다 못 만든 것이 결론(AA) — 대상 분포에 없는 것은
    양으로 못 메운다
  - `diagnose_labels`: 기준 모델이 아는 클래스 수가 데이터와 다르면 클래스
    대조를 끄고 위치만으로 진단한다. 안 그러면 멀쩡한 라벨을 전부 클래스
    오기입으로 부른다
  - `backend`: 유형별 도메인 강건성 표를 화면에 노출
- **다중 클래스 3-seed** (docs/21 AB) — L~AA가 전부 단일 관측이었다
- **자 비교에 오차막대** (docs/21 AC·AD) — 학습 시드를 바꿔 자를 3개씩 만들었다

### Fixed

- `aggregate_seeds`가 n=1일 때 표준편차를 0.0으로 써서 유의성 검사를
  통과시키던 것 → NaN
- `run_multi_seed`가 metrics CSV를 통째로 복사해, 이 실행과 무관한 조건의
  seed=42 값이 새 시드 측정값인 척 기록되던 것
- 실험용 조건(정제 부분집합)이 고객 리포트에 오류 유형으로 올라와 등급을
  통째로 누르던 것
- **"3 시드 자"가 시드를 안 바꾼 실험이었다** — 자는 전부 clean에서 나오는데
  clean에는 주입할 오류가 없어, 오류 시드를 바꿔도 같은 자가 나온다.
  `verify_seeds_differ()`가 args.yaml을 읽어 막는다

### 정정

- **Q의 "라벨 중복은 다중 클래스에서 조언이 뒤집힌다"가 틀렸다** —
  3-seed로 보니 0.88% ± 5.92(0.1σ)로, 뒤집힌 건 조언이 아니라 그 주장이었다

## 2026-09-02

### Added

- 깨끗한 기준 모델 없이 진단하기(docs/21 V) — self 자로도 중복 판정은 산다
- **자기 정제 실험**(docs/21 W·X) — 정제가 얻는 것보다 절반을 버리는 손해가
  크다. 800장에서 다시 재니 교환 비율이 7배 좋아졌지만 여전히 진다
- `check_consistency.py` — 오류 없이 조용히 틀리는 것들을 잡는 검사기
  (이미지/라벨 개수 불일치, 조건군 누락, 접미사 없는 경로)

### Fixed

- 산출물 경로를 데이터 규모별로도 분리 — 800장 실험이 400장 결과를 덮던 것
- 업로드 화면에 `class_mismatch` 한글 라벨이 빠져 있던 것

## 2026-08-29 ~ 08-31

제품의 뼈대가 생긴 구간. 데이터셋 업로드 → 박스 단위 진단 → 재검수 우선순위.

### Added

- **데이터셋 업로드 + 진단**(docs/21 C) — zip을 올리면 성능 패턴 DB와 비교
- **박스 단위 진단** — "어느 이미지의 어느 박스를 왜 다시 봐야 하는지"
- 오류 유형 확장: 누락·중복(21 → 27 조건), 다중 클래스(class_mismatch)
- **유형 신뢰도 프로파일** — 상수가 도메인을 타므로 데이터셋마다 골라 쓴다
- 신뢰도 가중 심각도 — 상위 10% 정밀도 91.7% → 99.7%
- 누락 판정 필터 — 유형 정밀도 27% → 98.5%
- 클래스 오기입 신뢰도 게이트 — 83.9% → 100%
- `--reuse-cache` — 20분 걸리던 평가가 2.2초
- 다중 시드 파이프라인과 집계된 오차막대

### Fixed

- `run_all --priority all`이 21개 조건 중 8개를 조용히 건너뛰던 것
- 재검수 대기열이 거꾸로 정렬돼 있던 것
- 실행 폴더를 하드코딩된 목록이 아니라 glob으로 찾도록(880MB 누락)
- **라벨 없는 이미지를 통째로 링크해 "객체 없는 배경"으로 학습되던 것** —
  섹션 S가 이걸로 무효가 됐고, 결론이 뒤집혔다

### 음성 결과

- 신뢰도는 중복 탐지에 도움이 안 된다
- 클래스 희소성이 취약도를 설명하지 못한다
- 재학습 검증에서 클래스 가중 정렬이 지지되지 않아 기본값을 되돌렸다

## 2026-08-19

### Added

- **OBB(회전 바운딩박스) 실험 파이프라인 추가** — 교수님 피드백의 핵심 지적
  ("회전 오류가 AABB 외접 박스 근사라 방향성이 없다")을 해결하는 신규 파이프라인.
  기존 21개 조건 AABB 결과는 완전히 보존하고 OBB 실험을 독립적으로 추가함.
  - `experiment/config.py`: OBB 전용 경로·조건 5개 추가
    (`obb_clean`, `obb_rot_m15/m7_5/p7_5/p15`)
  - `experiment/data_loader.py`: `to_yolo_obb_line()` 등 OBB GT 라벨 생성 함수 추가
    (KITTI AABB → polygon 포맷 변환, angle=0이 참값)
  - `experiment/error_injector.py`: `rotate_obb_poly()` 추가 — AABB는 회전 후 외접
    박스를 써서 방향성이 사라지지만, OBB는 polygon 꼭짓점 자체를 회전시켜 +θ/-θ가
    서로 다른 라벨로 저장됨. 검증: `rot+15°`와 `rot-15°`의 polygon이 다름을 확인.
  - `experiment/train_obb.py`, `evaluate_obb.py`, `run_obb.py` — OBB 전용
    파인튜닝(yolov8n-obb.pt)·평가·파이프라인 진입점 신규 생성
  - `backend/app/config.py`: `OBB_METRICS_CSV_PATH` 추가
  - `backend/app/routers/report.py`: `/api/obb/conditions` 엔드포인트 추가
    (metrics_obb.csv 없으면 빈 배열 반환 — 실험 전 대시보드가 깨지지 않도록)
  - `frontend/src/components/ObbComparisonChart.tsx`: AABB vs OBB 회전 저하율
    나란히 비교하는 차트 신규 생성. OBB 데이터 없으면 실행 안내 메시지 표시.
  - `frontend/src/App.tsx`, `api.ts`: OBB 섹션·API 호출 추가

### Next

- [ ] 로컬 CUDA 데스크탑에서 `python run_obb.py` 실행 (5조건 × 50epoch)
- [ ] 결과 확인: metrics_obb.csv 생성 후 대시보드 OBB 비교 차트에서 방향성 차이 확인

이 프로젝트의 개발 이력을 시간순으로 기록한다. 각 항목은 "무엇을 했는지"와
"왜 했는지"를 함께 남긴다. 의사결정의 배경(왜)은 [docs/06-decisions.md](docs/06-decisions.md)에
더 자세히 정리되어 있다.

## 2026-07-08 (4)

### Changed

- `experiment/config.py`의 `AIDA_DEVICE` 기본값을 `mps`(하드코딩된 M1 전용)에서
  `auto`로 변경. `resolve_device()`가 `cuda > mps > cpu` 순으로 이 머신에서 실제
  쓸 수 있는 디바이스를 자동 감지하도록 재작성
- **이유**: 팀원 로컬(M1 Mac, MPS)뿐 아니라 데스크탑(RTX 3050, CUDA)에서도 git
  clone 후 `.env` 수정 없이 동일하게 최적 디바이스로 학습이 돌아가야 함. 특정
  디바이스를 강제하고 싶으면 여전히 `AIDA_DEVICE=cuda`/`mps`/`cpu`로 명시 가능

## 2026-07-08 (3)

### Added

- `backend/tests/` — pytest 기반 API 테스트 12개. `conftest.py`가 결정론적 픽스처
  CSV(`fixtures/metrics.csv`)로 `METRICS_CSV_PATH`를 오버라이드해 실제 실험 결과가
  바뀌어도 테스트가 흔들리지 않도록 함. `/api/health`, `/api/summary` 형태 검증,
  `/api/conditions`의 성능 저하율 계산, `/api/diagnose`의 재검수 우선순위 경계값
  (15%/8% 정확히 걸치는 값)·정렬·라벨 검증. 임계값 로직을 일부러 깨뜨려 테스트가
  실제로 실패하는지 확인 후 원복(뮤테이션 테스트로 유효성 검증)
- `experiment/tests/` — `error_injector.py`의 좌표 변형 함수 pytest 8개 (GPU/학습
  불필요, 순수 함수). 가로·세로 스케일링, 회전 변형, YOLO↔픽셀 좌표 왕복, 경계값
  clamp 검증. 특히 `test_apply_rotation_nonzero_angle_enlarges_non_square_box`는
  김성호 교수님이 지적한 한계("회전은 항상 박스를 키우는 방향으로만 작용해 방향성이
  없다")를 코드 수준 회귀 테스트로 문서화함 — 향후 회전 오류 구현을 바꾸면 이
  테스트가 먼저 반응하도록 설계
- `README.md`에 테스트 실행 방법 추가

## 2026-07-08 (2)

### Added

- 발표자료(`../AIDA_발표자료_v2(결과반영).pptx`, 저장소 밖 원본 옆에 새 버전으로 저장,
  원본 미변경) 4곳 업데이트
  - 15번 슬라이드: 실측 결과 그래프(`docs/assets/experiment-results-priority1.png`)와
    핵심 발견 2줄 추가
  - 16번 슬라이드: "확정 진단" 문구를 교수님 피드백대로 "확률 기반 추정 + 재검수
    가이드" 톤으로 조정
  - 19번 슬라이드: 플레이스홀더였던 "제품 스크린샷"을 실제 대시보드 스크린샷으로 교체
  - 31번과 32번(신규 삽입) 슬라이드 사이에 "도입 효과 추정(ROI)" 슬라이드 신규 추가 —
    NIA·한국데이터경제신문·AWS/GCP 공개 자료 기반 가정 시나리오로 재검수 인건비·재학습
    비용 절감 추정치 제시 (추정치임을 각주로 명시)
  - 32~40번 슬라이드 페이지 번호를 33~41로 순연

### Fixed

- pptx 스킬의 `clean.py`가 슬라이드25의 차트(`ppt/charts/chart1.xml`)를 "미참조 파일"로
  오판해 삭제하는 버그 발견 (관계 Target이 절대경로라 스크립트가 인식 못함) → 편집 시
  `clean.py` 생략하고 직접 pack하는 방식으로 우회

## 2026-07-07

### Added

- 프로젝트 초기 스캐폴딩 생성 (`backend/`, `frontend/`, `docs/`)
- **backend**: FastAPI 앱 구성
  - `app/main.py` — 앱 진입점, CORS 설정
  - `app/models.py` — Pydantic 응답 모델 (`ConditionMetric`, `DatasetSummary`, `ErrorTypeReport`, `DiagnosisResult`)
  - `app/routers/report.py` — `/api/summary`, `/api/conditions`, `/api/diagnose` 3개 엔드포인트
  - `app/data/metrics.csv` — 실험 결과 목업 데이터 (7개 조건, 실제 실험 완료 전까지 사용)
  - Python venv 생성 및 `fastapi`, `uvicorn`, `pandas` 설치
  - 로컬에서 3개 엔드포인트 curl 테스트 통과 확인
- **frontend**: Vite + React + TypeScript 스캐폴딩
  - `recharts`, `axios` 설치
  - `src/types.ts` — 백엔드 모델과 1:1 대응하는 TypeScript 인터페이스
  - `src/api.ts` — axios 기반 API 클라이언트
  - `src/components/QualityScoreCard.tsx` — 품질 점수 카드
  - `src/components/PerformanceChart.tsx` — 조건별 성능 저하 막대그래프 (recharts)
  - `src/components/ErrorReportTable.tsx` — 오류 유형별 재검수 우선순위 표
  - `src/App.tsx`, `src/App.css`, `src/index.css` — 대시보드 레이아웃, 발표자료와 동일한
    흑백+형광(#CCFF00) 톤으로 스타일링
  - `tsc -b && vite build` 빌드 통과 확인 (recharts Tooltip 타입 에러 1건 수정)
  - `npm run dev` 로컬 구동 확인
- `docs/` 폴더 생성: 프로젝트 개요, 기술 원리, 아키텍처, 실험 설계서, API 레퍼런스,
  용어사전, 의사결정 로그, 사업화 로드맵 문서화
- 발표자료(PPTX) 40장 초안 완성 (문제정의→솔루션→차별성→시장분석→BM→실행계획→팀)
  - 1차: 네이비/시안 컬러 테마 → 2차: 형광 라임 하이라이터 스타일 → 3차: 형광 사용을
    5장으로 최소화, 박스형 카드 반복 제거하고 에디토리얼 텍스트 레이아웃으로 전환
  - 슬라이드 13(핵심 기술 원리)을 4단계(가상이미지·참값 생성 / 에러 바운딩박스 생성 /
    모델 학습과 평가 자동화 / 오류 판단)로 확정 반영
- 기술 검증 실험 설계서 확정: KITTI Car 클래스, 7개 오류 조건(가로/세로 ±30%,
  회전각 ±15°), YOLOv8n 파인튜닝 기반

### Decided

- 자세한 배경은 `docs/06-decisions.md` 참고. 요약:
  - 특허(10-2664201) 청구항과 정합성 확인 → 가로/세로/회전각을 주 오류 유형으로 확정
  - 신규 이미지 생성 없이 KITTI 기존 이미지+라벨을 참값으로 재활용하기로 결정
  - 대상 클래스는 Car 단일 클래스로 한정
  - MVP 실험은 유료 AI API 없이 로컬(M1 Mac, MPS)/무료 크레딧만으로 진행
  - MVP 데모는 FastAPI + React/TypeScript 웹 대시보드로 결정

### Next

- [ ] `experiment/` 폴더: KITTI 다운로드 → 라벨 변환 → 오류 라벨 생성 스크립트 구현
- [ ] YOLOv8n 파인튜닝 13회 실행 (핵심 7개 우선, 세분화 6개 이어서), `metrics.csv`를 실제 결과로 교체
- [ ] 교수님(김성호, 영남대 전자공학과) 기술 검토 회신 반영
- [ ] 2차 예선(2026-07-09~10) 발표 리허설

## 2026-07-07 (2)

### Changed

- 실험 조건 매트릭스를 7개 → **13개로 재확정** (기준선 + 가로±30/±15% + 세로±30/±15% +
  회전각±15/±7.5°). 이유는 `docs/06-decisions.md`의 "2026-07-07 (2)" 항목 참고
- `backend/app/data/metrics.csv`를 7행 → 13행 목업 데이터로 갱신, API 응답 정상 확인
- `docs/03-experiment-design.md` 4번 섹션을 13개 조건 매트릭스로 갱신, 실행 순서(핵심
  7개 우선 → 세분화 6개)를 시간 리스크 관리 방안으로 명시

### Added

- `docs/08-professor-review-email.md` — 교수님께 보낼 기술 검토 요청 메일 완성본
  (본문 5개 핵심 질문 + 세부 사항은 첨부 자료로 분리)
- `docs/09-getting-started.md` — 새 세션/새 개발자가 프로젝트를 이어받을 때 가장
  먼저 읽는 진입점 문서 (완료된 것 / 다음 할 일 / AI 어시스턴트용 안내 포함)

## 2026-07-07 (3)

### Added

- `experiment/` 폴더 구현 (docs/03-experiment-design.md 5절 모듈 구성 그대로 반영)
  - `config.py` — 13개 조건 정의, SEED/EPOCHS 등 전역 설정 (스모크 테스트용
    `AIDA_N_TRAIN`/`AIDA_N_VAL`/`AIDA_EPOCHS` 환경변수 오버라이드 지원)
  - `download_kitti.py` — 라벨 zip(5.6MB)은 전체, 이미지 zip(12.5GB)은 HTTP
    Range 요청으로 Car 클래스 포함 프레임 중 필요한 만큼만 부분 다운로드
    (전체 다운로드 불필요, S3가 Accept-Ranges: bytes 지원함을 확인 후 구현)
  - `data_loader.py` — Car 클래스 추출, KITTI→YOLO 라벨 변환, SEED 고정 train/val 분할
  - `error_injector.py` — 13개 조건별로 라벨 30% 무작위 변형 (크기: 중심 고정
    스케일링, 회전: 꼭짓점 회전 후 축정렬 재계산), 조건별 데이터셋을 파일 단위
    심볼릭 링크로 구성해 이미지 중복 없이 디스크 절약
  - `train.py`, `evaluate.py`, `run_all.py` — YOLOv8n 파인튜닝(우선순위 1/2/all
    선택 실행) 및 평가, 결과를 `backend/app/data/metrics.csv` 스키마로 자동 갱신
  - venv 생성 후 `AIDA_N_TRAIN=16 AIDA_N_VAL=8 AIDA_EPOCHS=1`로 전체 파이프라인
    (다운로드→전처리→에러 주입→학습→평가→metrics.csv 갱신) 스모크 테스트 통과 확인

### Fixed

- `error_injector.py`에서 조건별 `images/` 디렉토리를 통째로 심볼릭 링크했더니
  ultralytics가 data.yaml 경로를 `.resolve()`할 때 원본 디렉토리로 역참조되어
  라벨을 못 찾는 문제 발견 → 디렉토리 대신 파일 단위 심볼릭 링크로 변경해 해결
- `evaluate.py`의 `model.val()`에 `project`/`name` 미지정 시 프로젝트 루트에
  `runs/detect/val`이 새로 생겨버리는 문제 → `config.RUNS_DIR` 하위로 고정

### Next

- [ ] 실제 KITTI 다운로드(400+120장) 및 13개 조건 전체 학습·평가 실행
      (핵심 7개 우선 → 세분화 6개, 예상 2~4시간) — 사용자 확인 후 진행
- [ ] 교수님(김성호) 기술 검토 회신 반영
- [ ] 2차 예선(2026-07-09~10) 발표 리허설

## 2026-07-07 (4)

### Added

- `docs/10-competition-brief.md` — 공식 공고문(방위사업청·국방과학연구소·민군협력진흥원,
  2026-06-09) 기준 대회 전체 일정, 1차/2차 예선 평가기준 배점표, 본선 시상 내역,
  지원 내용(바우처 멘토링, MVP 제작비 50만원, 기술이전 특전 등) 정리

### Fixed

- `docs/07-roadmap.md`의 "2차 예선 통과 시 전원 수상" 메모가 공식 공고와 다름을
  확인 후 정정 (본선은 발표·시연 기반 경쟁 평가이며 최종 결과로 학생부/일반부
  각 6팀만 수상). 배경은 `docs/06-decisions.md` "2026-07-07 (3)" 항목 참고

## 2026-07-08

### Added

- **핵심 7개 조건 실제 학습·평가 완료** — KITTI Car 400장(학습)+120장(평가)로
  clean/width±30/height±30/rot±15 학습, `backend/app/data/metrics.csv`에 실측
  결과 반영 (목업 데이터 → 실제 데이터로 최초 교체)
- `docs/11-professor-feedback.md` — 김성호 교수님(영남대 AVIL) 기술 검토 회신 정리
  및 2차 예선 전 대응 방안 (포지셔닝 문구 변경, 회전각 오류 한계 인정, IoU 기반
  강도 지표 제안, ROI 정량화 등)
- `docs/12-experiment-results.md` — 실측 결과 분석: (1) 세 오류 유형 모두 성능
  저하 확인, (2) 회전각 오류가 가장 크고 양방향으로 대칭적인 저하를 보임 (교수님
  피드백의 "축정렬 재계산이 방향성을 지운다" 지적과 일치), (3) Precision은 거의
  불변, Recall만 뚜렷이 하락 — mAP 외 핵심 지표로 Recall 확인
- `docs/assets/experiment-results-priority1.png` — PPT에 바로 쓸 수 있는 조건별
  mAP@0.5·Recall 저하율 그래프

### Changed

- `run_all.py --priority 1` 실행 중 `clean`(50epoch) 완료 후 나머지 6개 조건은
  25epoch로 단축 (실제 학습 속도가 예상보다 느려 시간 리스크 관리 목적). 배경과
  트레이드오프는 `docs/06-decisions.md` "2026-07-08" 항목,
  `docs/12-experiment-results.md` "한계" 절 참고

### Next

- [ ] 교수님 피드백 액션 아이템 반영 (발표 스크립트 포지셔닝 변경 등)
- [ ] 세분화 6개 조건 실행 (`run_all.py --priority 2`)
- [ ] `clean` 25epoch 재실행으로 공정 비교 기준 확보 (선택)
- [ ] 2차 예선(2026-07-09~10) 발표 리허설
