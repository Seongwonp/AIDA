# 인계 — 2026-09-16 (Q-A 준비: 모집단·비교 모드·경로 드라이런)

## 한 줄

다음 평가의 1차 질문을 **Q-A(후보 생성 포함), 보조를 Q-C(누락 층 기준선)** 로 정하고
(사용자 결정), 그것을 잴 수 있는 **최소 구현과 경로 드라이런**을 마쳤다. **판정은 하지
않았고 어떤 성능도 말하지 않는다.** 지금 막힌 곳은 코드가 아니라 **라이선스 원문 확인(W1)과
사전 등록(W5)** 이고, 둘 다 사용자 몫이다.

## 순서표 상태 — [next-work-2026-09-15.md](next-work-2026-09-15.md)

| ID | 무엇 | 상태 |
|---|---|---|
| W0 | 시작 점검 | **끝** — 세 층 검사·CI·보호 자료 해시 |
| W1 | 라이선스 원문 확인 | **[사용자] 미착수** — 여기서 막힌다 |
| W2 | 1차 비교 질문 | **끝** — Q-A 1차 + Q-C 보조 |
| W3 | 최소 구현 | **끝** — 모집단·방법별 예산·비교 모드·무작위 표본 |
| W4 | 경로 드라이런 | **끝** — 실제 추론 → 묶음 → 합성 판정 → 집계 |
| W5 | 사전 등록 | **[사용자] 미착수** — W1 뒤 |
| W6·W7 | 판정 / 집계·감사 | 미착수 |

## 무엇이 바뀌었나

**진단** (`experiment/label_diagnosis.py`, `diagnose_labels.py`)

- 규칙에 걸린 후보 외에 **모든 기존 라벨**(`all_labels`, 라벨마다 `label_iou`)과 **누락 필터 전
  미매칭 예측**(`unmatched_predictions`, 확신도·삼켜짐)을 결과 파일에 적는다.
- 모집단을 안 모은 실행은 그 키 자체가 없다 — 옛 결과와 구분되고, 그런 진단으로는 후보 생성
  비교를 못 돌린다.

**평가** (`backend/app/routers/evaluation.py`)

| 무엇 | 규칙 |
|---|---|
| 후보 출처 | `aida_candidate` / `label`(규칙 밖) / `unmatched_prediction`(필터가 버린 것) |
| 병합 | 같은 대상은 **한 번만** 얼린다. 기존 라벨은 (이미지, 라벨 번호), 누락은 (이미지, 상자)로 잇는다 |
| 상자 충돌 | 상자가 같은 미매칭 예측이 둘이면 **얼리기를 거부한다**(조용히 덮어쓰지 않는다) |
| 방법 | 기존 라벨 층 `aida`·`iou_baseline`·`all_label_iou`, 누락 층 `aida`·`unmatched_confidence` |
| 예산 | 방법마다 **자기 모집단에서** 상위 N. 모집단이 작으면 남는 예산을 안 쓴다 |
| 비교 모드 | 기본 `within_aida_candidates`(prelim1과 같은 질문) / `candidate_generation_included`(Q-A·Q-C) |
| 무작위 표본 | 규칙 밖 라벨에서 K건, 씨앗 필수. 판정 대상에 더해질 뿐 **어느 방법의 상위 N도 바꾸지 않는다** |

**집계** (`experiment/evaluation/`)

- 짝지은 차이·부트스트랩은 **기본적으로 후보 집합이 같아야** 한다(정렬 효과). 후보 생성까지
  포함한 비교는 `require_same_candidates=False`로 **밝혀야** 돌고, 짝지음 단위는 그때
  **이미지 묶음**이다. 결과에 `same_candidate_set`이 남는다.

**옛 묶음은 아무것도 안 바뀐다.** prelim1의 후보 이름·지문(`e996e262…`)과 진단 파일 해시를
다시 확인했다.

## W4 드라이런 결과 — `experiment/planning_evidence/w4_dryrun.json`

prelim1의 300장을 **읽기만 해서** 다시 진단하고 결과는 저장소 밖(`D:/AIDA-eval/dryrun/w4/`)에
썼다.

| | |
|---|---|
| 재현 | AIDA 후보 **331건이 prelim1과 완전히 동일**(순위·심각도·상자) |
| 모집단 | 라벨 1280건 중 규칙 밖 **973건**, 필터 전 미매칭 예측 322건(AIDA 누락 24 → 298건을 필터가 버렸고 296건이 확신도 0.70 미만) |
| 묶음 | 후보 1602건, `snapshot.json` 약 426KB |
| 판정량 (자리값 N=90·K=50) | **388건** = 기존 라벨 298 + 누락 90. prelim1은 186건이었다. 5.02초로 치면 약 32분(1인 참고값) |
| 경로 | 세 내보내기(후보 안 / 후보 생성 포함 / 누락 보조)와 집계·부트스트랩 통과 |

**합성 판정으로 돌렸으므로 수율·차이는 싣지 않았다.** N·K·씨앗은 **자리값**이고 진짜 값은 W5에서
사용자가 정한다.

## 검사 (2026-09-16 데스크탑, 직접 실행)

experiment **459 통과** · backend **302 통과**(건너뜀 0) · 프론트는 바꾸지 않아 안 돌렸다(직전 183).
구현 전에 실패하는 검사를 먼저 확보했고 기대값은 손으로 계산했다. 경계는
[testing-boundary.md](testing-boundary.md) "후보 생성 포함 비교".

**외부 리뷰(코파일럿 CLI) 두 번**을 받아 둘을 반영했다 — 상자가 같은 예측의 조용한 덮어쓰기(고침),
짝지음 검사가 약하다는 지적(이미지별 부호가 갈리는 세계로 보강). "무작위 표본을 방법 순위에서
빼라"는 지적은 **채택하지 않았다** — 표본은 `all_label_iou`의 모집단이라 빼면 그 방법의 순서가
무작위 추출에 흔들린다. 규칙은 "표본이 예산을 쓰지 않는다"이고 문구를 고쳤다.

## 커밋 (전부 `main`에 푸시)

| 커밋 | 내용 |
|---|---|
| `59dd2016` · `a763b066` | W0 점검 기록, W2 결정 |
| `218f9bdf` | 진단이 모집단을 낸다 |
| `344041c8` | 묶음·예산·비교 모드·무작위 표본 |
| `f56f312d` · `ce8f1372` · `e90dcd66` | 계약 문서와 검사 경계 |
| `99dcaf24` | 상자가 같은 미매칭 예측 거부 |
| `cd64b652` | 이미지 묶음 짝지음, W4 기록 |
| `04057734` | 짝지음 검사 보강 |

## 노트북에서 할 수 있는 것과 없는 것

`backend/app/data/uploads/`, `experiment/data/`, `experiment/runs/`는 gitignore라 `git pull`로
안 따라온다. **D 드라이브의 보호 복사본과 드라이런 산출물도 데스크탑에만 있다.**

| 노트북에서 | |
|---|---|
| 된다 | 세 층 검사 전부(GPU 불필요), 문서, 코드, 사전 등록 초안, 라이선스 확인 기록 |
| 안 된다 | 진단 추론(가중치·이미지 없음), W4 같은 드라이런, 보호 자료 해시 대조, 실제 판정 |

노트북에서 검사를 돌릴 때는 CI와 같은 의존성만 깐 가상환경을 쓰고, **돌린 수를 직접 적는다.**
`backend/tests/test_uploads_dir_agreement.py`의 외부 드라이브 검사 1건은 노트북에서 건너뛴다.

## 다음 — 둘 다 사용자 결정

1. **W1 라이선스 원문.** BDD100K(우선 후보)의 "비영리·교육·연구" 조건이 목적 A(비상업 연구
   시제품)에 맞는지, `yolov8n.pt` 가중치 조건, KITTI CC BY-NC-SA 원문. 확인 목록은 순서표 W1 절,
   배경은 [evaluation-data.md](evaluation-data.md) "이 단계의 목적".
2. **W5 사전 등록.** 데이터셋과 개발/최종 경계, 클래스 범위, **N·K·씨앗**, 1차 지표와 Δ 방침,
   판정자. 판정량은 드라이런 수치로 가늠할 수 있다 — N=90·K=50이면 388건이었다.
   형식은 [prelim1-preregistration.md](prelim1-preregistration.md).

**W2가 정해졌으므로 새 기능은 W5 전까지 더 만들지 않는다.**

## 남은 위험

- ~~판정 화면 부하를 안 쟀다.~~ **서버 쪽은 저녁에 쟀다(2부, 병목 아님).** 실제 브라우저에서
  목록을 넘기며 이미지를 그리는 부하는 여전히 안 쟀다 — W6 전에 점검 평가로 한 번 열어 본다.
- **실제 추론으로 모집단이 나오는지는 드라이런 한 번뿐이다.** CI는 ultralytics를 못 돌린다.
- ~~`ruler.json`이 순위 버전별이 아니다(순서표 S3).~~ **저녁에 고쳤다(2부 `6d308729`).**
- prelim1의 300장·시간 파일럿·KITTI 학습 데이터·COCO val2017 Car는 **소비된 개발 데이터**다
  ([evaluation-data.md](evaluation-data.md) 원장).

## 명령 모음

검사 (노트북·데스크탑 공통):

```
cd backend && ./venv/Scripts/python.exe -m pytest tests -q
cd experiment && ./venv/Scripts/python.exe -m pytest tests -q
cd frontend && npm test && npm run typecheck && npm run lint && npm run build
```

드라이런 재현 (데스크탑 전용, GPU 1분 미만). 출력 경로가 이미 있으면 다른 경로를 준다:

```
cd experiment
./venv/Scripts/python.exe diagnose_labels.py --dataset-dir <데이터셋 폴더> --out <저장소 밖 경로>/label_diagnosis.json
```

`--dataset-dir`는 **읽기만 한다** — 업로드 폴더의 진단 결과를 덮어쓰지 않는다.

## 2부 — 저녁, 결정이 필요 없는 일 (같은 날)

사용자가 공부하는 동안 **사용자 결정 없이 할 수 있는 일만** 했다. W1·W5 전에 새 기능을 만들지 않는다는
원칙은 지켰다 — 아래 코드 변경은 결함 수정과 검사뿐이다.

| 커밋 | 무엇 |
|---|---|
| `6d308729` | **S3 결함 수정** — 자 기록(`ruler.json`)을 순위 버전마다 따로 둔다. 전에는 다른 프로파일로 v2를 돌리면 v1 결과를 열 때 v2의 자가 붙었고, 그 뒤 얼린 v1 묶음의 지문에 틀린 자가 들어갔다. v1은 `ruler.json` 그대로, 자기 기록이 없는 옛 v2 결과는 공유 파일을 빌리지 않고 "자를 모른다"(`None`) |
| `6e7531f1` | **S4 조사** — "access violation" 출력은 실행마다 다른 자리(순수 파이썬 프레임 포함)에서 찍히고, `test_config.py`만 돌리면 안 나오며, 테스트 프로세스에 torch·ultralytics·cv2가 올라와 있다. `-p no:faulthandler`로도 같은 459건 통과. 처리된 네이티브 예외 출력으로 **추정**, 조치 안 함 |
| `10731c90` | **판정 API 부하** — 1602건 묶음에서 시작 190ms, 판정 목록 388건 22ms(67KB), 전체 판정 저장 18ms 이하, 내보내기 25ms. 서버는 병목이 아니다. **브라우저 렌더링은 안 쟀다** |
| `a073064e` | **사전 등록 초안** [qa-preregistration-draft.md](qa-preregistration-draft.md) — 사용자 결정 D1~D8을 빈칸으로, 코드가 이미 고정한 규칙, v1/v2 선택이 1차 비교의 뜻을 어떻게 바꾸는지, N·K별 판정량표, 표본 K의 Wilson 구간 폭. 판정 목록에 출처·표본 여부·확신도가 새지 않는다는 검사 추가 |
| `a3ce7593` | **판정자 둘 이상 설계 초안** [multi-adjudicator-design.md](multi-adjudicator-design.md) — 지침 보정, 독립 판정, 층별 일치도, 조정 방식 세 가지, 판정자별 저장, 옛 파일 호환. 구현 안 함 |

코드 변경(`6d308729`, `a073064e`의 검사)은 **코파일럿 CLI 리뷰**를 받았다 — 이상 없음.

**사전 등록 초안에서 새로 드러난 것 (결정 재료):**

- **누락 층이 늘 N건을 쓴다.** 필터 전 미매칭 예측(드라이런 322건)이 N보다 많아서, 보조 질문(Q-C)이
  판정량의 약 4분의 1을 차지한다. 누락 층 예산을 따로 두려면 **구현이 먼저 필요하다**(지금은 N 하나).
- **D4(AIDA 쪽 순위 버전)가 1차 비교의 뜻을 바꾼다.** v1이면 "후보 생성 + v1 순서"를 같이 재고,
  v2면 기존 라벨 층에서 v2 순서가 곧 `1 − label_iou`라 **후보 생성만** 잰다.
- 표본 K=50이면 판정 오류 비율 10%일 때 Wilson 95% 구간이 약 4~21%다 — K가 작으면 "규칙이 놓친 비율"은
  넓게만 말할 수 있다.

**검사 (저녁, 직접 실행):** backend 308 통과(건너뜀 0), experiment 459 통과, frontend 183 통과,
typecheck·lint·build exit 0. 보호 자료: 시간 파일럿 44개 파일·prelim1 보호 결과 4개 SHA-256 일치,
prelim1 진단 파일이 보호 복사본과 같고 묶음 지문 `e996e262…` 그대로.

**다음은 여전히 사용자 결정이다 — W1(라이선스 원문), 그다음 초안의 D1~D8(W5).**
