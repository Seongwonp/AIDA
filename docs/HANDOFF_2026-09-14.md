# 인계 — 2026-09-14 (prelim1 불리한 결과 감사)

## 한 줄

사전 등록한 1차 탐색 비교(N=90)에서 **현재 제품 순서가 단순 IoU 기준선보다 나빴다**
(고유 오류 4 대 14). 이 결과를 감사했고 **숫자는 맞았다.** 기록 누락 결함 하나를 고쳤고,
원인 가설과 선택지를 적었다. **순위를 바꾸지 않았고 새 평가를 하지 않았다.**

## 커밋

| 커밋 | 내용 |
|---|---|
| `5cf0e998` | 세션 끝 판정 결함, 판정·기록 대조, 판정 완료 시 기록 즉시 전송 |
| `181f29f4` | N=90 독립 산술 감사, 부트스트랩 짝지음·누락 id 검사, 사후 실패 분석 스크립트 |
| (이 문서와 같은 커밋) | 결과 문서 정정, 실패 분석·결정 문서, 데이터 원장, timing4 정정 |

## 확인한 것

- **1차 지표:** N=90 하나. 10~80은 사후 깊이 진단. hit 후보 = 고유 오류 (AIDA 4, 기준선 14).
- **185 대 186:** 마지막(186번째) 후보 `L683817e4141f5335`의 기록이 서버에 없다. 세션 판정이
  StrictMode의 앞쪽 `session_ended`에 속아 잘린 세션을 온전하다고 셌다 — 고쳤다.
  재계산: timing5 `s_plan` 5.02초 그대로, timing4 3.58 → 3.81초, prelim1 `timing_usable` 거짓.
- **독립 감사:** 제품 집계와 모두 같다. 90위 동점 없음, 한 오류당 후보 1건.
- **부트스트랩:** 짝지음·중복 추출·서버 형식 id·예산·씨앗·반복을 확인했다.
- **사후 분석 (가설):** width만 상대 문턱으로 승격돼 AIDA 상위 90이 전부 width였다. width 94건
  중 오류 4, 비승격 68건 중 오류 14. 기준선이 찾은 14건은 AIDA 순서 130~297위에 있었다.

## 검사 (2026-09-14 데스크탑, 코드 변경 뒤 직접 실행)

experiment 416 passed · backend 261 passed · frontend 177 passed · typecheck·lint·build exit 0.
이 문서 커밋은 문서만이라 링크 확인만 했다.

## 보존 상태

prelim1 원본(`uploads/30512dfcbfbb`), 보호 복사본(`D:/AIDA-eval/prelim/prelim1_30512dfcbfbb`),
timing1~5, 사용자 데이터셋 5개를 고치지 않았다. 감사 산출물은 보호 폴더의 `audit/`에 새로 썼다.

## 사용자가 정할 것

1. ~~선택지 A~D 중 어느 방향을 탐색할지~~ **정했다 — C안 구조 + 임시 A안** (아래 2부).
2. 다음 비교에 쓸 **손대지 않은 데이터셋** — 법률 검토와 라이선스 원문 확인이 먼저다
   ([`next-evaluation-proposal.md`](next-evaluation-proposal.md) 3·5절).
3. 그 비교의 사전 등록(N·방법·씨앗·1차 지표·Δ 방침)을 판정 전에 커밋.

## 2부 — 데이터셋 진단과 후보 순위 분리 (같은 날)

[`adr-ranking-separation.md`](adr-ranking-separation.md). **v2는 평가되지 않은 시험 버전이다.**

| 커밋 | 내용 |
|---|---|
| `476bc45b` | ADR |
| `8bc6ce38` | 순위 버전 v1·v2, 결과 메타데이터·층 안 순위, 버전별 파일, 다른 버전 덮어쓰기 거부 |
| `6ae9870b` | API·평가 묶음·내보내기·집계·화면에 순위 버전. 옛 파일은 v1, 버전 없는 묶음의 지문 불변, 버전 섞인 집계 거부 |
| (이 문서와 같은 커밋) | 다음 평가 제안, 제품 가치 문서 정리 |

- **v1** `aida_v1_systematic_boost` — 기본값, 동작 그대로.
- **v2** `aida_v2_candidate_iou` — 기존 라벨 `1 − label_iou`(없으면 멈춤), 누락 심각도, 층 사이 점수
  비교 없음, 화면 목록은 층 순위 교차 배치(평가 안 한 표시 규칙). 데이터셋 판정으로 다시 매기지 않는다.
- **호환:** prelim1 묶음 지문을 새 코드로 읽기 전용 재계산 — 보호 복사본·업로드 사본 모두 `e996e262…`
  일치, v1으로 읽힘. 보호 결과 파일 4개 SHA-256 일치.
- **검사:** experiment 440 passed · backend 279 passed(건너뜀 0) · frontend 183 passed ·
  typecheck·lint·build exit 0. 실험 검사 중 `test_config.py`의 하위 프로세스에서 `faulthandler`가
  "access violation" 스택을 찍지만 검사는 통과한다 — 이번 변경과 무관한 파일이고 원인은 보지 않았다.
- **남은 위험:** `ruler.json`이 버전별이 아니다(다른 프로파일로 v1·v2를 돌리면 자 기록이 덮인다).
  한 묶음에 v1·v2 두 순서를 얼리는 기능은 없다(다음 평가에 필요).

## 다시 돌리는 명령

`experiment/` 폴더에서:

```
./venv/Scripts/python.exe audit_prelim1_n90.py
./venv/Scripts/python.exe analyze_prelim1_failure.py
```

두 스크립트는 산출물이 이미 있으면 덮어쓰지 않고 멈춘다. 보호 폴더
`D:/AIDA-eval/prelim/prelim1_30512dfcbfbb/`가 있어야 돈다.
