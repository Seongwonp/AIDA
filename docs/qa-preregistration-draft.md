# Q-A 사전 등록 **초안** — 후보 생성 포함 비교 (값 미정)

> **이것은 사전 등록이 아니다.** 빈칸(`[결정]`)이 남은 초안이다. 사용자가 빈칸을 채우면 새 이름의
> 확정본으로 옮겨 **판정 전에 커밋·푸시**한다. **빈칸이 하나라도 남은 채로 판정 화면을 열지 않는다.**
> 형식은 [prelim1-preregistration.md](prelim1-preregistration.md)를 따른다.
>
> 이 초안이 정한 것은 W2 결정(1차 Q-A, 보조 Q-C)과 **이미 코드에 들어간 규칙**뿐이다
> ([evaluation-adjudication-design.md](evaluation-adjudication-design.md) "모집단과 비교 모드").
> 선택지마다 무엇이 달라지는지는 적었지만 **고르지 않았다.**

## 0. 사용자가 정할 것 — 한눈에

| # | 무엇 | 선행 | 이 초안의 절 |
|---|---|---|---|
| D1 | 데이터셋·라이선스 확인 결과·개발/최종 경계 | W1 | 2 |
| D2 | 클래스 범위 | D1 | 2 |
| D3 | 표본 이미지 수와 표집 씨앗, 연속 프레임 묶음 규칙 | D1 | 2 |
| D4 | AIDA 쪽 순위 버전 (v1 / v2) | — | 3 |
| D5 | 판정 예산 N, 무작위 표본 K, 두 씨앗 | D3 | 5 |
| D6 | 누락 층(Q-C)을 같은 세션에 넣을지 | D5 | 5 |
| D7 | 1차 지표·구간 방법·Δ 방침 | — | 6 |
| D8 | 판정자 (1인이면 "탐색"으로 적는다) | — | 5·7 |

## 1. 목적

| | |
|---|---|
| **1차 (Q-A)** | AIDA 규칙이 만든 후보를 AIDA 순서로 본 것 대 **모든 기존 라벨**을 `1 − label_iou`로 본 것 — 같은 검수량 N에서 판정자가 오류로 본 고유 라벨 수 |
| **보조 (Q-C)** | AIDA 누락 후보 대 **필터 전 미매칭 예측 전부**를 확신도순으로 — 기술 통계만 |
| **표본 층** | AIDA 규칙 밖 기존 라벨의 무작위 표본 K에서 판정 오류 비율 — 규칙이 놓친 비율의 추정 |

**이 비교가 답하는 것:** "기존 라벨 쪽에서 AIDA 규칙 엔진이 순진한 전체 라벨 IoU 검수보다 더
나은 곳을 먼저 보게 하는가." 여기서도 지면 제품은 누락 탐지 쪽으로 좁혀진다
([next-work-2026-09-15.md](next-work-2026-09-15.md) W2).

**`[결정 D8]`** 판정자가 1인이면 이 비교는 **탐색**이다 — prelim1처럼 성공·실패를 주장하지 않는다.

## 2. 데이터 — `[결정 D1·D2·D3]`

| 항목 | 값 | 비고 |
|---|---|---|
| 데이터셋 | `[결정 D1]` | W1 라이선스 원문 확인 결과를 [evaluation-data.md](evaluation-data.md) "이 단계의 목적"에 적은 뒤 |
| 라이선스 확인 기록 | `[결정 D1]` | 원문 URL·확인 날짜·짧은 인용. 법적 판단은 적지 않는다 |
| 개발/최종 경계 | `[결정 D1]` | 최종 평가 몫을 이 비교에서 열지 않는다 |
| 클래스 범위 | `[결정 D2]` | Car 하나로 좁히면 클래스 정의 차이(Van·Cyclist)를 대부분 피한다 |
| 표본 이미지 수·표집 씨앗 | `[결정 D3]` | 이미지 이름 전부를 명세에 남긴다 |
| 연속 프레임 묶음 | `[결정 D3]` | **구현됨(2026-09-16).** 데이터셋 폴더의 `groups.json`(이미지 → 묶음)을 묶음을 만들 때 얼리고 `group_id`로 내보낸다. `nuimages_to_yolo.py`가 주행 기록으로 이 파일을 쓴다. val 표집은 `nuimages_eval_sample.py`(기록마다 최대 `per_log`장, **사전 등록 커밋이 없으면 val을 열지 않는다**) |
| 제외 | 고정 | 소비된 개발 데이터 전부 — [evaluation-data.md](evaluation-data.md) 원장 |
| 자 | `[결정 D1·D2]` | 클래스 범위에 맞는 자. 가중치 SHA-256을 명세에 |

**보존.** 판정에 쓰는 정확한 파일(이미지·라벨·`ruler.json`·진단·`snapshot.json`)을 저장소 밖에 읽기
전용으로 복사하고 SHA-256을 명세에 남긴다 — prelim1과 같다.

## 3. 제품 경로 — `[결정 D4]`

| 단계 | 경로 |
|---|---|
| 업로드 | `POST /api/datasets/upload` |
| 진단 | `POST /api/datasets/{id}/diagnose-labels?ranking=[결정 D4]` — 결과에 `all_labels`·`unmatched_predictions`가 **있어야 한다** |
| 묶음 | `POST .../evaluations` `{evaluation_id, shuffle_seed, judge_budget, ranking_version, random_sample_size, random_sample_seed}` |

**D4 — AIDA 쪽 순위 버전.** 무엇을 재는지가 달라진다.

| 선택 | 1차 비교가 재는 것 | 주의 |
|---|---|---|
| `aida_v1_systematic_boost` | 후보 생성 **과** v1 순서(계통 유형 승격)를 함께 | prelim1에서 v1 순서가 기준선보다 나빴다 — 지면 원인이 생성인지 순서인지 갈리지 않는다 |
| `aida_v2_candidate_iou` | **후보 생성만** — 기존 라벨 층에서 v2 순서가 곧 `1 − label_iou`라 두 방법의 차이는 "AIDA 규칙으로 거른 뒤 IoU 순" 대 "거르지 않고 IoU 순"뿐이다 | v2는 평가되지 않은 시험 버전이다. 이 비교는 v2의 순서를 검증하지 않는다 |

## 4. 방법 — 고정 (W2)

| 층 | 방법 | 모집단 | 순서 |
|---|---|---|---|
| 기존 라벨 (1차) | `aida` | AIDA 후보 | D4 버전의 제품 순위 |
| 기존 라벨 (1차) | `all_label_iou` | 모든 기존 라벨 | `1 − label_iou` |
| 누락 (보조) | `aida` | AIDA 누락 후보 | 제품 순위 |
| 누락 (보조) | `unmatched_confidence` | 필터 전 미매칭 예측 전부 | 확신도 |

**판정 뒤에 방법을 더하거나 빼지 않는다.** `iou_baseline`(AIDA 후보 안 재정렬)은 1차에 넣지 않는다
— 넣으려면 판정 전에 보조로 적는다. D4가 v2면 코드가 그 비교를 막는다.

## 5. 판정 규칙 — `[결정 D5·D6·D8]`

**고정 (코드 규칙):**

- 판정 대상 = 층마다 **방법별 상위 N의 합집합** ∪ 무작위 표본 K. 겹치면 한 번만 판정한다.
- 방법의 모집단이 N보다 작으면 **남는 예산을 안 쓴다.**
- 무작위 표본은 **규칙 밖 라벨에서만** 뽑고, 판정 대상에 더해질 뿐 어느 방법의 상위 N도 바꾸지 않는다.
- 상자가 같은 미매칭 예측이 둘 이상이면 묶음이 **얼려지지 않는다** — 그때는 진단부터 본다.
- 가림: 점수·순위·유형·출처(`source`)·표본 여부를 판정 화면에 보내지 않는다. 기존 라벨인지 누락인지는
  과제가 달라 가리지 않는다.
- 판정 지침은 [manual-timing-pilot.md](manual-timing-pilot.md). `hold`를 쓸 수 있다.

**`[결정 D5]`** N · K · `shuffle_seed` · `random_sample_seed` · 판정 묶음 나누기.

**판정량 가늠 — prelim1 300장 드라이런 진단 기준 참고값.** 새 데이터셋에서는 다르다. **묶음을 얼린
뒤 실제 수를 다시 적는다.** 시간은 timing5의 `s_plan` 5.02초(판정자 1인·연속 120건 1회)로 친 것이다.

| N | K | 판정 대상 | 기존 라벨 | 누락 | 표본 중 합집합 밖 | 5.02초 기준 |
|---|---|---|---|---|---|---|
| 30 | 0 | 119 | 89 | 30 | 0 | 약 10분 |
| 30 | 50 | 168 | 138 | 30 | 49 | 약 14분 |
| 60 | 0 | 234 | 174 | 60 | 0 | 약 20분 |
| 60 | 50 | 282 | 222 | 60 | 48 | 약 24분 |
| 60 | 100 | 329 | 269 | 60 | 95 | 약 28분 |
| 90 | 0 | 342 | 252 | 90 | 0 | 약 29분 |
| 90 | 50 | 388 | 298 | 90 | 46 | 약 32분 |
| 90 | 100 | 435 | 345 | 90 | 93 | 약 36분 |
| 120 | 50 | 490 | 370 | 120 | 41 | 약 41분 |
| 120 | 100 | 535 | 415 | 120 | 86 | 약 45분 |

읽는 법:

- **누락 층이 늘 N건을 차지한다** — 필터 전 미매칭 예측(322건)이 N보다 많기 때문이다. 보조 질문이
  판정량의 약 4분의 1을 쓴다. **`[결정 D6]`** 같은 세션에 넣을지. 누락 층만 예산을 따로 두려면
  **구현이 먼저 필요하다**(지금은 N 하나가 모든 층에 쓰인다).
- 표본 K는 대부분 합집합 밖에서 뽑혀 거의 그대로 판정량에 더해진다.
- prelim1은 한 묶음이 관측한 지속 판정 길이(120건 1회)를 넘지 않게 두 묶음으로 나눴다. 300건이
  넘으면 **세 묶음 이상**을 생각해야 한다.

**표본 K가 주는 정밀도 — Wilson 95% 구간 참고값** (규칙 밖 라벨의 판정 오류 비율 추정).

| K | 관측 2% | 5% | 10% | 20% |
|---|---|---|---|---|
| 30 | 1/30 → 1~17% | 2/30 → 2~21% | 3/30 → 3~26% | 6/30 → 10~37% |
| 50 | 1/50 → 0~10% | 2/50 → 1~13% | 5/50 → 4~21% | 10/50 → 11~33% |
| 100 | 2/100 → 1~7% | 5/100 → 2~11% | 10/100 → 6~17% | 20/100 → 13~29% |
| 200 | 4/200 → 1~5% | 10/200 → 3~9% | 20/200 → 7~15% | 40/200 → 15~26% |

이 구간은 **판정자 한 사람의 판정 비율**에 대한 것이다. 참 오류 비율이 아니다.

## 6. 판정 뒤 분석 — `[결정 D7]`

**고정 (절차):**

1. 내보내기 저장 — 기존 라벨 층
   `?methods=aida,all_label_iou&scope=labelled_candidates&mode=candidate_generation_included`,
   누락 층 `?methods=aida,unmatched_confidence&scope=missing_candidates&mode=candidate_generation_included`.
2. `importer.load_export` → `summary.summarise`로 방법마다 검수량 N에서 **고유 오류 수**·hit 후보 수·
   보류 수·결정된 수, 그리고 `method_population`을 적는다.
3. 기존 라벨 층의 짝지은 차이와 구간은 **`require_same_candidates=False`**(이미지 묶음 짝지음)로 낸다.
   결과의 `same_candidate_set: false`를 보고서에 그대로 적는다 — **정렬 효과로 부르지 않는다.**
4. 표본 층: `random_sample: true`인 줄만으로 판정 오류 비율과 구간. **방법별 고유 오류 수와 합치지 않는다.**
5. 누락 층: 두 방법의 hit·보류 수. `descriptive_only`다.

**`[결정 D7]`**

| 항목 | 선택지 | 비고 |
|---|---|---|
| 1차 지표 | 검수량 N에서 고유 오류 수의 차이 (`all_label_iou` − `aida`) | 부호 방향을 미리 적는다 |
| 1차 검수량 | N 하나 | prelim1처럼 다른 깊이는 **사후 진단**으로만 |
| 구간 | 이미지 묶음 부트스트랩 — 씨앗·반복 수 | prelim1은 42·2000 |
| 표본 층 구간 | Wilson 또는 부트스트랩 | 미리 하나를 고른다 |
| Δ 방침 | Δ를 정하고 판정 규칙을 쓸지 / prelim1처럼 판정하지 않을지 | Δ가 없으면 성공·실패를 적지 않는다 |
| 연속 프레임 | 묶음 단위(이미지 / 장면) | D3과 같이 |
| 층별 보고 | 상자 높이 경계(기본 0-30·30-60·60-120·120+)와 보고할 방법 | **구현됨** — `evaluation/strata.py`. 방법의 상위 N을 **전체에서 고른 뒤** 층으로 나눈다. 기술통계만, 경계는 판정 전에 고정 |

## 7. 지키는 약속 — 고정

- 판정을 본 뒤 **N·K·방법·씨앗·판정 규칙·1차 지표·Δ 방침을 바꾸지 않는다.**
- 결과가 불리해도 **이 데이터로 순서·문턱을 다시 세지 않는다.** 이 데이터는 판정 뒤 소비된 개발
  데이터가 된다.
- **판정을 정답으로 부르지 않는다.** 한 사람의 판정은 일관성을 보일 뿐 정확성이 아니다.
- 이 단계의 결과를 **상업적 주장·영업 자료에 쓰지 않는다**(목적 A).
- 후보 생성 포함 비교를 **정렬 효과**로, 표본 층 비율을 **참 오류 비율**로 부르지 않는다.

## 8. 판정 전 점검 — 확정본에서 채울 표

| 점검 | 기대 | 결과 |
|---|---|---|
| `all_candidates` 길이 = `total_in_queue` | 같다 | |
| `all_labels` 길이 = 진단 요약의 `total_labels` | 같다 | |
| `unmatched_predictions`가 있다 | 있다 | |
| 묶음이 얼려졌다(상자 중복 거부가 안 났다) | 200 | |
| 출처별 후보 수 | `aida_candidate`·`label`·`unmatched_prediction` | |
| 판정 대상 = 방법별 상위 N 합집합 ∪ 표본 | 손으로 다시 센 수와 같다 | |
| 표본 K건이 전부 `source=label` | 맞다 | |
| 판정 목록에 점수·순위·유형·출처·표본 여부가 없다 | 없다 | |
| 섞인 순서가 씨앗대로 같은가 | 두 번 만들어 같다 | |
| 합성 판정으로 내보내기 → 집계 → 부트스트랩 | 통과 (9절) | |

## 9. 합성 판정 경로 확인

소비된 개발 데이터로는 이미 돌렸다 — [next-work-2026-09-15.md](next-work-2026-09-15.md) W4,
`experiment/planning_evidence/w4_dryrun.json`. 판정량 API 부하는 `w4_load_check.json`(1602건 묶음에서
판정 목록 22ms, 전체 판정 저장 18ms 이하). **확정본에서는 실제로 얼린 묶음으로 한 번 더** 메모리에서
돌리고, 실제 평가 폴더에 판정 파일이 생기지 않았는지 확인한다.

## 10. 커밋 뒤, 판정 전에 할 일

1. 가림 화면을 **점검용 평가**(같은 씨앗·예산의 별도 ID)로 열어 본다. 실제 평가에 기록이 남지 않게.
2. 목록이 prelim1보다 훨씬 길다 — 점검용 평가에서 **실제 브라우저로 목록을 넘기며 이미지가 그려지는지**
   본다(서버 부하는 쟀지만 화면 렌더링은 안 쟀다).
3. 점검 탭을 모두 닫고 늦게 오는 기록 전송을 기다린 뒤, 실제 평가에 `adjudications.json`·
   `activity.jsonl`이 없는지 본다.
4. 마지막 판정 뒤에는 기록 전송이 끝났는지 확인한다(prelim1의 186 대 185 사례).
