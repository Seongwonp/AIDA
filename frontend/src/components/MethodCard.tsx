/**
 * 이 진단이 무엇을 하고 무엇을 못 하는지 적는 카드.
 *
 * "근거" 탭에 지표와 차트만 있어서, 숫자는 보이는데 **그 숫자가 어떻게 나온
 * 것인지**가 없었다. 실험 방식과 한계를 제품 안에 적어두지 않으면, 고객이
 * 진단 결과를 실제보다 확정적인 것으로 읽는다.
 *
 * 여기 적힌 내용은 docs/21의 실측을 요약한 것이다.
 */

const METHOD = [
  {
    q: "정답 없이 어떻게 라벨이 틀렸다고 하나",
    a: "이미 학습된 모델의 예측을 자로 삼아 라벨과 박스 단위로 대조합니다. 예측과 크게 어긋나는 라벨을 '의심'으로 올릴 뿐, 확정 오류로 판정하지 않습니다. 그래서 결과는 판정이 아니라 재검수 우선순위입니다.",
  },
  {
    q: "그 자가 틀리면 어떻게 되나",
    a: "유형마다 다르게 무너집니다. 기하 오류(크기·위치)는 예측 박스를 정밀한 잣대로 쓰므로 자가 흔들리면 같이 무너지고, 라벨 누락은 '확신 있는 예측이 있는데 라벨이 없다'는 판정이라 자가 상자를 정확히 그리지 못해도 버팁니다. 아래 유형별 신뢰도 표가 그 실측치입니다.",
  },
  {
    q: "얼마나 믿을 수 있나",
    a: "오류를 일부러 주입한 27종 조건에서 재봤습니다. 도메인이 맞는 기준 모델이면 상위 10%의 정밀도가 94.0%(시드 7개 평균)입니다. 다만 아예 다른 데이터셋에서는 26.0%까지 떨어졌습니다 — 그래서 어느 기준 모델을 썼는지 결과에 함께 표시합니다.",
  },
  {
    q: "무엇을 못 하나",
    a: "라벨을 고쳐주지 않습니다. 고치려면 정답을 알아야 하는데 그건 이 방법이 가진 정보가 아닙니다. 또 지금 실측은 KITTI와 COCO의 차량 중심이라, 전혀 다른 도메인에서의 수치는 아직 없습니다.",
  },
];

export function MethodCard() {
  return (
    <section className="card">
      <h2>이 진단은 무엇을 하고, 무엇을 못 하나</h2>
      <dl className="method-list">
        {METHOD.map(({ q, a }) => (
          <div key={q} className="method-item">
            <dt>{q}</dt>
            <dd>{a}</dd>
          </div>
        ))}
      </dl>
      <p className="report-caveat">
        수치의 근거는 저장소의 <code>docs/21-next-plan.md</code>에 절 단위로
        남겨두었습니다. 결론이 뒤집힌 경우 원문을 지우지 않고 정정 주석을 답니다.
      </p>
    </section>
  );
}
