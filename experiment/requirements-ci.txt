# GPU 없이 돌릴 수 있는 실험 검사용 의존성 (docs/24 B).
#
# experiment/requirements.txt에는 torch·ultralytics가 들어 있어 CI에서 못 쓴다.
# 그런데 **정답지를 만드는 코드**(error_injector)와 진단 규칙(label_diagnosis)은
# 순수 계산이라 GPU가 필요 없다. 그 66건이 지금까지 CI에서 한 번도 안 돌았다.
#
# 이 목록의 최소성은 빈 가상환경에서 실제로 확인했다 — 이것만 깔고 66건 통과.
pytest
pillow          # config·error_injector가 이미지 크기를 읽는다
pyyaml          # data.yaml 생성
python-dotenv   # config가 .env를 읽는다
